{
  "config": {
    "step": {
      "user": {
        "title": "Add a button mapping",
        "description": "Detect single/double clicks and long presses on a trigger entity. Pick target lights to toggle per gesture, or paste a full YAML action sequence in the advanced fields.",
        "data": {
          "name": "Name",
          "trigger_entity": "Trigger entity",
          "mode": "Input mode",
          "click_window": "Click window (ms)",
          "long_press_time": "Long press time (ms)",
          "fire_events": "Also fire button_actions_gesture events",
          "single_click_targets": "Single click → toggle these",
          "single_click_action": "Single click action (YAML, overrides targets)",
          "double_click_targets": "Double click → toggle these",
          "double_click_action": "Double click action (YAML, overrides targets)",
          "long_press_targets": "Long press → toggle these",
          "long_press_action": "Long press action (YAML, overrides targets)"
        }
      }
    },
    "abort": {
      "already_configured": "This trigger entity is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Edit button mapping",
        "description": "Edit any field, including the trigger entity. The advanced YAML fields are prefilled with the current action so you can copy or change it; a YAML action overrides the toggle targets.",
        "data": {
          "name": "Name",
          "trigger_entity": "Trigger entity",
          "mode": "Input mode",
          "click_window": "Click window (ms)",
          "long_press_time": "Long press time (ms)",
          "fire_events": "Also fire button_actions_gesture events",
          "single_click_targets": "Single click → toggle these",
          "single_click_action": "Single click action (YAML, overrides targets)",
          "double_click_targets": "Double click → toggle these",
          "double_click_action": "Double click action (YAML, overrides targets)",
          "long_press_targets": "Long press → toggle these",
          "long_press_action": "Long press action (YAML, overrides targets)"
        }
      }
    },
    "error": {
      "already_configured": "Another mapping already uses this trigger entity."
    }
  },
  "selector": {
    "mode": {
      "options": {
        "momentary": "Momentary (pulses back, 2 transitions per press)",
        "toggle": "Toggle (state holds, 1 transition per press)"
      }
    }
  },
  "services": {
    "reload": {
      "name": "Reload",
      "description": "Reload all YAML-configured Button Actions mappings without restarting Home Assistant."
    }
  }
}
